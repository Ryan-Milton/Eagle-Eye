import * as React from "react";
import { cn } from "@/lib/utils";

export function Sidebar({ className, header, children, footer }: { className?: string; header?: React.ReactNode; children: React.ReactNode; footer?: React.ReactNode }) { return <aside className={cn("flex h-full w-72 shrink-0 flex-col overflow-hidden border-r border-line bg-panel", className)}>{header ? <div className="shrink-0 border-b border-line p-4">{header}</div> : null}<nav className="neo-scrollbar min-h-0 flex-1 overflow-y-auto p-3">{children}</nav>{footer ? <div className="shrink-0 border-t border-line p-4">{footer}</div> : null}</aside>; }
export function SidebarGroup({ className, label, children }: { className?: string; label?: string; children: React.ReactNode }) { return <div className={cn("mb-6 grid gap-1", className)}>{label ? <div className="neo-kicker px-2 py-2 text-muted-foreground">{label}</div> : null}{children}</div>; }
export function SidebarLink({ className, active, icon, children, ...props }: React.ComponentProps<"a"> & { active?: boolean; icon?: React.ReactNode }) { return <a className={cn("flex min-h-10 items-center gap-3 border border-transparent px-2 font-mono text-sm text-muted-foreground hover:border-line hover:bg-background hover:text-foreground", active && "border-signal bg-signal text-signal-foreground", className)} {...props}>{icon ? <span className="[&_svg]:size-4">{icon}</span> : null}<span>{children}</span></a>; }
